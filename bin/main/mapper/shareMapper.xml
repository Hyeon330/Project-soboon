<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.semiproject.soboon.dao.ShareBrdDAO">
	<insert id="shareInsert">
		insert into board(userid, title, content, category, thumbnailImg, img1, img2, img3)
		value(#{userid},#{title},#{content}, #{category}, #{thumbnailImg}, #{img1}, #{img2}, #{img3})
	</insert>
	<select id="shareListSelect">
		select 
	</select>
</mapper>