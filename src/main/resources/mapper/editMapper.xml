<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.semiproject.soboon.dao.EditDAO">
	<select id="selectEdit" resultType="MemberVO">
		select userid, username, userpwd, nickname, tel, address, email from member where userid = #{param1}
	</select>
	<select id="nicknameCheck" resultType="int">
		select count(nickname) from member where nickname=#{param1}
	</select>
	<update id="editUpdate">
		update member set userpwd = #{userpwd}, nickname = #{nickname}, username = #{username}, email = #{email}, address = #{address}, tel = #{tel}
		where userid = #{userid}
	</update> 
	<delete id="delMember">
		delete from member where userid=#{param1}
	</delete>
</mapper>